## TF - Tarefa Final - Aula 4

**Disciplina:** Implementação de servidor e nuvem (cloud)  
### Aula: 4 - Volumes e Persistência de Dados
### Objetivo do TF: Avaliar a compreensão da diferença entre Bind Mounts e Volumes Nomeados, e a proficiência na configuração da persistência de dados para ambientes de desenvolvimento e produção.

### Orientações:

1.  Responda às questões teóricas com base no conteúdo da aula.
2.  Execute os comandos práticos no seu terminal **WSL/Linux**.
3.  As respostas devem ser objetivas e baseadas nos comandos práticos.

### Questão 1: Efemeridade vs. Persistência (Teórica)

Um desenvolvedor está usando um contêiner PostgreSQL para um projeto, mas não configurou nenhum Volume. Após fazer `docker stop` e `docker rm` no contêiner, ele percebe que todos os dados foram perdidos.

a) **Explique** qual camada de sistema de arquivos do contêiner foi destruída ao executar o `docker rm` e por que ela é considerada **efêmera**.
b) Qual mecanismo o Docker oferece para garantir que os dados sobrevivam à remoção do contêiner?

### Questão 2: Bind Mounts (Prática e Teórica)

Você está desenvolvendo uma aplicação em Python no seu Host (WSL) e precisa que as mudanças de código sejam refletidas instantaneamente no contêiner, sem ter que reconstruir a imagem.

a) **Prática:** Assumindo que você está no diretório `~/projeto_python/` no seu WSL, execute os comandos necessários para:
    1. Criar um diretório de trabalho `~/projeto_python/`.
    2. Rodar um contêiner da imagem `nginx:latest` mapeando o diretório **atual** do seu Host para o diretório `/usr/share/nginx/html` do contêiner. (Utilize o comando Linux para obter o caminho absoluto `$(pwd)`).
    *(Liste o comando `docker run` completo utilizado).*

b) **Teórica:** Qual é a principal desvantagem ou risco de usar o **Bind Mount** para persistir dados de um Banco de Dados em um ambiente de produção?

### Questão 3: Volumes Nomeados (Prática e Teórica)

Para um ambiente de produção, você precisa garantir a persistência segura para os dados de um banco de dados MongoDB.

a) **Prática:** No seu terminal **WSL/Linux**, execute os comandos para:
    1. Criar um **Volume Nomeado** chamado `mongo_data_prod`.
    2. Listar todos os volumes nomeados existentes para confirmar a criação.
    3. **Inspecionar** o volume `mongo_data_prod` para encontrar a localização real (`Mountpoint`) onde o Docker armazena os dados no Host.
    *(Liste os comandos utilizados e o campo `Mountpoint` encontrado).*

b) **Teórica:** Por que o **Volume Nomeado** é a forma preferida para persistir dados de bancos de dados em produção, em contraste com o Bind Mount? (Foque em como o Docker gerencia o ciclo de vida).

### Questão 4: Limpeza de Volumes (Prática e Teórica)

Você parou e removeu todos os seus contêineres, mas percebe que alguns volumes antigos estão ocupando espaço no disco.

a) **Prática:** Qual comando você deve utilizar para **remover todos os volumes** que não estão sendo usados por nenhum contêiner no momento?
    *(Liste o comando utilizado).*

b) **Teórica:** Se você tentar usar o comando `docker volume rm meu_volume` em um volume que ainda está sendo utilizado por um contêiner em execução, qual será o resultado?

### Questão 5: Uso do `tmpfs` (Teórica)

Em um cenário onde uma aplicação gera logs extremamente sensíveis ou temporários que **não devem ser gravados de forma persistente no disco rígido do Host**, qual tipo de montagem deve ser utilizada?

**Explique** por que essa montagem é a solução ideal neste caso e qual é a principal limitação desse tipo de armazenamento.
```